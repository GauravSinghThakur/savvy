"""
backend.app package

Purpose:
- Acts as the Python package root for the FastAPI application.
- Keeps application modules (routers, models, dependencies) organized under `backend/app/`.

Notes:
- Import side effects are avoided here; app creation should live in `main.py`.
"""

